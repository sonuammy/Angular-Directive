export class Employee {

    //3.1 ngFor with Object Array

    // empid: number;
    // empname: string;
    // salary: number;
    // constructor(a, b, c)
    // {
    // this.empid = a;
    // this.empname = b;
    // this.salary = c;
    // }
    
    //3.2 ngFor using Constructor 
    
    constructor(public empid: number, public empname: string, public salary: number) {
    }
}

